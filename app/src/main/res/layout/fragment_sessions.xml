<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingStart="@dimen/session_padding_horizontal"
    android:paddingEnd="@dimen/session_padding_horizontal"
    android:paddingTop="@dimen/session_padding_top"
    android:paddingBottom="@dimen/session_padding_bottom">

    <!-- Título -->
    <TextView
        android:id="@+id/titleSession"
        android:layout_width="@dimen/space_none"
        android:layout_height="wrap_content"
        android:text="@string/session_title"
        android:textAppearance="@style/TextAppearance.Material3.HeadlineMedium"
        android:textStyle="bold"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Buscador -->
    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/inputSearch"
        style="@style/Widget.Material3.TextInputLayout.OutlinedBox"
        android:layout_width="@dimen/space_none"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_m"
        android:hint="@string/session_hint_search"
        app:layout_constraintTop_toBottomOf="@id/titleSession"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/textSearch"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:imeOptions="actionSearch"
            android:inputType="text" />
    </com.google.android.material.textfield.TextInputLayout>

    <!-- Lista -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerSession"
        android:layout_width="@dimen/space_none"
        android:layout_height="@dimen/space_none"
        android:clipToPadding="false"
        android:paddingTop="@dimen/space_m"
        android:paddingBottom="@dimen/sessions_list_bottom_padding"
        app:layout_constraintTop_toBottomOf="@id/inputSearch"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Loading -->
    <ProgressBar
        android:id="@+id/progressLoading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="@id/recyclerSession"
        app:layout_constraintBottom_toBottomOf="@id/recyclerSession"
        app:layout_constraintStart_toStartOf="@id/recyclerSession"
        app:layout_constraintEnd_toEndOf="@id/recyclerSession" />

    <!-- Empty -->
    <TextView
        android:id="@+id/textEmpty"
        android:layout_width="@dimen/space_none"
        android:layout_height="wrap_content"
        android:visibility="gone"
        android:text="@string/session_empty"
        android:textAlignment="center"
        android:textAppearance="@style/TextAppearance.Material3.BodyLarge"
        app:layout_constraintTop_toTopOf="@id/recyclerSession"
        app:layout_constraintBottom_toBottomOf="@id/recyclerSession"
        app:layout_constraintStart_toStartOf="@id/recyclerSession"
        app:layout_constraintEnd_toEndOf="@id/recyclerSession" />

    <!-- Error -->
    <TextView
        android:id="@+id/textError"
        android:layout_width="@dimen/space_none"
        android:layout_height="wrap_content"
        android:visibility="gone"
        android:text="@string/session_error_generic"
        android:textAlignment="center"
        android:textAppearance="@style/TextAppearance.Material3.BodyLarge"
        app:layout_constraintTop_toTopOf="@id/recyclerSession"
        app:layout_constraintBottom_toBottomOf="@id/recyclerSession"
        app:layout_constraintStart_toStartOf="@id/recyclerSession"
        app:layout_constraintEnd_toEndOf="@id/recyclerSession" />

    <!-- FAB: solo + -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabAddSession"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/session_add_game"
        app:srcCompat="@drawable/ic_add"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginEnd="@dimen/space_m"
        android:layout_marginBottom="@dimen/space_m" />

</androidx.constraintlayout.widget.ConstraintLayout>